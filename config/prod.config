%% -*- erlang -*-
[
 "config/sys.config",
 {lager,
  [
   {log_root, "log"}
  ]},
  {blockchain_node, [
    {metrics, [block_metrics, txn_metrics, grpc_metrics]},
    {metrics_port, 9090}
  ]},
 {blockchain,
  [
   {base_dir, "data"}
  ]},
 "etc/node.config"
].
